
const defaultState = {
  dance:null,
  initialize:false
}

const dancesReducer = (state = defaultState,action) => {
  switch(action.type) {
    default:
      return state
  }
}

export default dancesReducer